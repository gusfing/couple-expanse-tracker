import { Category } from './types';

export const DEFAULT_BUDGET = 15000;
export const DEFAULT_CURRENCY = '₹';

export const INITIAL_EXPENSES = []; // Start empty

export const CATEGORIES = [
  Category.FOOD,
  Category.TRAVEL,
  Category.SHOPPING,
  Category.BILLS,
  Category.FUN,
  Category.OTHER
];
